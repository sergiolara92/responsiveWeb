en el repository. settings > pages > elegir la branch que queremos subir




dividir las paginas en tareas pequeñas.
al acaba runa tarea pequeña hacer commit y darle un nombre.
al final del dia otro commit general.
organizar en carpetas.
se puede hacer un css para cada html e incluso para cada seccion.
hacer un index.css --> @import /navbar.css
asi se importan todos los archivos css y en el html llamar al index.css
tambien los js hacer uno para cada pagina html.

los botones que hagan un console log. pero que no recarguen la pagina